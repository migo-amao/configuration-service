server:
  port: 9000

spring:
  application:
    name: configuration-service

  cloud:
    config:
      server:
        git:
          uri: https://github.com/migo-amao/configuration

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: 5672
    username: guest
    password: guest